import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1A56DB);
  static const primaryLight = Color(0xFFEEF2FF);
  static const secondary = Color(0xFF0E9F6E);
  static const secondaryLight = Color(0xFFDEF7EC);
  static const warning = Color(0xFFF59E0B);
  static const warningLight = Color(0xFFFEF3C7);
  static const danger = Color(0xFFE02424);
  static const dangerLight = Color(0xFFFDE8E8);
  static const purple = Color(0xFF7E3AF2);
  static const purpleLight = Color(0xFFEDE9FE);

  static const background = Color(0xFFF3F4F6);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);
  static const textMuted = Color(0xFF9CA3AF);
  static const border = Color(0xFFE5E7EB);
  static const divider = Color(0xFFF3F4F6);

  static const navBar = Color(0xFF111827);
  static const navBarSelected = Color(0xFF1A56DB);

  // Legacy aliases (kept for backward compatibility)
  static const textDark = textPrimary;
  static const textGrey = textSecondary;
  static const cardBg = surface;
  static const error = danger;
  static const success = Color(0xFF10B981); // separate green
}
